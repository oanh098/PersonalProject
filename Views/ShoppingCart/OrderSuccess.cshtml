@using PersonalProject.Models.ShoppingCartProcess
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model dynamic


<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            <div class="mb-4">
                <i class="bi bi-check-circle text-success" style="font-size: 4rem;"></i>
                <h2 class="mt-3">Order Confirmed!</h2>
                @* <p class="text-muted">Thank you for your order, @Model.User.Identity.Name.</p> *@
            </div>

            @* <div class="card shadow-sm border-0 p-4">
                <h4 class="mb-4">Order Summary</h4>
                <div class="text-start">
                    <p><strong>Order ID:</strong> @Model.ItemToPurchase.cart</p>
                    <p><strong>Total Paid:</strong> @Model.TotalAmount.ToString("C")</p>
                    <p><strong>Payment Method:</strong> @Model.PaymentMethod</p>
                    <p><strong>Shipping to:</strong> @Model.ShippingAddress</p>
                </div>
            </div> *@

            <div class="mt-4">
                <a asp-controller="Home" asp-action="Index" class="btn btn-outline-primary">Continue Shopping</a>
                <a asp-controller="Account" asp-action="OrderHistory" class="btn btn-primary">View My Orders</a>
            </div>
        </div>
    </div>
</div>